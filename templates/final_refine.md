# Final Refine

Address the review findings in the most recent review output under `.forgeroom/outputs/`.

Read the review (`{{review}}` when provided), make the real code changes in the worktree to resolve each finding, and avoid unrelated changes. Ensure the project still builds and its checks pass.

Summarize how you addressed each finding and write it to `.forgeroom/outputs/{{step_index}}_{{step_id}}.md`.
