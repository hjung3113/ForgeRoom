# Implement Slice

Implement exactly this one slice of the plan:

{{slice}}

Read the task context in `.forgeroom/context/` and prior outputs under `.forgeroom/outputs/`. Follow the repository's `AGENTS.md` / `CLAUDE.md` conventions. Make the real code change in the worktree, scoped to this slice only.

Summarize what you changed and write it to `.forgeroom/outputs/{{step_index}}_{{step_id}}.md`.
