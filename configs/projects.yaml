forgeroom:
  path: /Users/hyojung/Desktop/2026/ForgeRoom
  default_branch: main
  package_manager: pnpm
  default_workflow: quick
  allowed_workflows:
    - quick
    - full
    - hotfix
  template_dir: null
  commands:
    lint: pnpm lint
    typecheck: pnpm typecheck
    test: pnpm test:unit
  maintainers:
    discord_user_ids: ["388212532697104384"]
    github_logins:
      - hjung3113
  # ProjectRoom seam (ADR-028, Phase 1.5). Reserved sections — optional, no
  # migration. Only the keys below are parsed today; everything else is ignored
  # until its owning phase lands. Left commented so behavior is inert until the
  # Discord reverse-map (#84) and OpenClaw override (#85) wire them up.
  # discord:
  #   channel_id: "C_FORGEROOM"   # Phase 2A: channel → project reverse-map
  # openclaw:
  #   room: forgeroom             # Phase 2B: session namespace root
  #   agents:                     # role → OpenClaw agent id
  #     planner: fr-forgeroom-planner
  #     implementer: fr-forgeroom-impl
  #     reviewer: fr-forgeroom-reviewer
  # mastra:
  #   expose_operator_tools: true # Phase 2C: Mastra operator console
